import styled from 'styled-components';
import { Root, Trigger, Portal, Content, Close } from '@radix-ui/react-popover';

export const StyledPopover = styled(Root)``;

export const StyledPopoverTrigger = styled(Trigger)``;

export const StyledPopoverPortal = styled(Portal)``;

export const StyledPopoverContent = styled(Content)``;

export const StyledPopoverClose = styled(Close)``;
