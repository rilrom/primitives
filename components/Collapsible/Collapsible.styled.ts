import styled from 'styled-components';
import { Root, Trigger, Content } from '@radix-ui/react-collapsible';

export const StyledCollapsible = styled(Root)``;

export const StyledCollapsibleTrigger = styled(Trigger)``;

export const StyledCollapsibleContent = styled(Content)``;
