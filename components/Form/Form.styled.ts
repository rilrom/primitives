import styled from 'styled-components';
import {
  Root,
  Field,
  Label,
  Control,
  Message,
  ValidityState,
  Submit,
} from '@radix-ui/react-form';

export const StyledForm = styled(Root)``;

export const StyledFormField = styled(Field)``;

export const StyledFormLabel = styled(Label)``;

export const StyledFormControl = styled(Control)``;

export const StyledFormMessage = styled(Message)``;

export const StyledFormValidityState = styled(ValidityState)``;

export const StyledFormSubmit = styled(Submit)``;
